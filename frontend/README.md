# Frontend (not yet built)

The Next.js + TypeScript dashboard is planned starting Phase 2/3 (see the root
[README](../README.md#roadmap)). Phase 1 is backend-only: use the FastAPI endpoints directly
(`/docs` for interactive API docs) until the dashboard exists.
